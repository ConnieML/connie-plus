# Production Environment for v2.connie.plus
# Multi-tenant deployment with AWS Secrets Manager

# Okta Configuration
NEXT_PUBLIC_OKTA_ISSUER=https://trial-2094636.okta.com
NEXT_PUBLIC_OKTA_CLIENT_ID=0oat7hjmxbdY525p9697
NEXT_PUBLIC_OKTA_REDIRECT_URI=https://v2.connie.plus/callback

# Security Configuration
NEXT_PUBLIC_ALLOWED_PARENT_ORIGIN=https://flex.twilio.com
NEXT_PUBLIC_FLEX_DOMAIN=https://nss.connie.team

# AWS Configuration (use IAM role in production)
AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY should be set via IAM role
# Only set these if not using IAM role

# Default Twilio Configuration (fallback only)
# These will be replaced by AWS Secrets Manager lookups
TWILIO_ACCOUNT_SID=REPLACE_WITH_DEFAULT_ACCOUNT
TWILIO_AUTH_TOKEN=REPLACE_WITH_DEFAULT_TOKEN

# Node Configuration
NODE_ENV=production